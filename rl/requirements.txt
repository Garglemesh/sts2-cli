# Python dependencies for the StS2 RL trainer.
# Versions pinned to what was verified working on 2026-06-21 (Python 3.11+ / CUDA).
# On Linux, the default PyPI torch wheel bundles CUDA — no separate CUDA install needed,
# only the host NVIDIA driver + nvidia-container-toolkit when running in Docker.
torch==2.12.1
numpy==2.5.0
gymnasium==1.3.0
stable-baselines3==2.9.0
sb3-contrib==2.9.0
tensorboard==2.20.0
